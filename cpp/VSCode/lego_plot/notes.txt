Install GLFW development libraries
run:
/# sudo apt-get install libglfw3-dev


-------------------------------
build project:
root directory
/# mkdir -p build
/# cd build
/# cmake ..
/# make
/# ./[project name defined in CMakeLists]

Required package:
/# sudo apt update && sudo apt install -y libwayland-dev libxkbcommon-dev libwayland-cursor0 libwayland-egl1

Required package: // trashy
GLM (OpenGL Mathematics) library
/# sudo apt update && sudo apt install libglm-dev

Required package:
GLAD modern OpenGL features
/# sudo apt update && sudo apt install libglad-dev
